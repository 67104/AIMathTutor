# ============================================================================
# AI Math Tutor — Python dependencies (desktop dev + Android build)
# Python 3.12+
# ----------------------------------------------------------------------------
# Install (desktop dev):  pip install -r requirements.txt
# Note: on Android, packaging is controlled by buildozer.spec "requirements".
#       Tesseract/OpenCV behave differently on-device (see docs/PROBLEMS.md).
# ============================================================================

# --- UI framework ---
kivy==2.3.0
kivymd==1.2.0

# --- Math engine ---
sympy==1.13.3
numpy==2.1.3

# --- Image / OCR ---
pillow==11.0.0
opencv-python==4.10.0.84        # desktop; on Android use 'opencv' recipe
pytesseract==0.3.13             # requires Tesseract binary installed on desktop

# --- Progress charts: drawn in pure Kivy (app/widgets/bar_chart.py) ---
#     No Matplotlib dependency — keeps the Android APK small and buildable.

# --- Optional local backend (only if truly needed; disabled by default) ---
fastapi==0.115.5
uvicorn==0.32.1

# --- Dev / test ---
pytest==8.3.3
pytest-mock==3.14.0

# ----------------------------------------------------------------------------
# NOT pip-installed, but required on the DEV machine for OCR:
#   * Tesseract OCR engine (Windows: https://github.com/UB-Mannheim/tesseract)
#     Set services/ocr_service.py -> TESSERACT_CMD to its install path.
# ----------------------------------------------------------------------------
